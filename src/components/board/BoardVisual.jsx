import { BoardVisualStyle } from './style';

const BoardVisual = () => {
    return (
        <BoardVisualStyle>
            <h2>Board</h2>
        </BoardVisualStyle>
    );
};

export default BoardVisual;
